spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dataagent?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.dataagent: DEBUG
